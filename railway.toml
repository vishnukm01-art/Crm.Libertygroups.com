[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma generate && npx prisma db push --skip-generate && npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
